ExpirationUsersPass: 
  # At 00:00:00am every day
  cron: "0 0 0 ? * * *"
  # days of life of the pasword
  timeLifePass: 180
  # period of days before the password expires where reminders are sent
  noticesDaysBefore: 10
  #  maximum number of days of inactivity
  maxInactiveDays: 30 
Authentication:
   limitFailedAttemp: 5
   numberLastEntriesToCheck: 5
ResetUserPass:
   # hours of validity of the temporary password
   hours: 7
   # At second :00 of minute :00 of every hour
   cron: "0 0 * ? * * *"